version: "3"

services:
  nodejs:
    build:
      context: .
      dockerfile: Dockerfile
    image: nextjs
    ports:
      - 5000:5000
    environment:
      NODE_ENV: production
    deploy:
      restart_policy:
        condition: on-failure
